import { render } from '@testing-library/react-native';
import * as React from 'react';

import { App } from './App';

describe('App', () => {
  it('should work', () => {
    render(<App />);
  });
});
